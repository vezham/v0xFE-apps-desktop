# wjdlz/NOTE: ci@1.0.5
name: TVA | wf

on:
  push:
    branches: [timeline, deploy/dev, deploy/qa, deploy/preview, deploy/prod]
    paths:
      - '.github/workflows/tva.yml'
      - 'v/handbooks/ci.md'
    tags:
      - 'ci@*' # ci@suites-hq@1.0.5, ci@suites-hq@1.0.5-dev, ...

jobs:
  deployer:
    name: TVA | ${{github.ref_name}}
    uses: vezham/v0xFE-deployer/.github/workflows/tva.yml@timeline
    secrets: inherit
    permissions:
      contents: write
    with:
      CALLER: ${{github.ref_name}}
